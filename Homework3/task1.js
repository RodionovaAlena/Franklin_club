let num = promt('Введите целое положительное число'); // пользователь вводит, например, 38

alert((String.fromCharCode(num))); // выводится символ "&", т.к. в таблице ASCII он расположен под номером 38